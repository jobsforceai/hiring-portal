"use client";

export default function ActionBar() {
  // A subtle bottom glow / brand stripe (optional, can place timer here later)
  return (
    <div className="pointer-events-none fixed inset-x-0 bottom-0 h-24 bg-gradient-to-t from-violet-600/10 to-transparent" />
  );
}
